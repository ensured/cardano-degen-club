import { Toaster } from "react-hot-toast"

import Notify from "@/components/Notify"

const page = () => {
  return (
    <div>
      <Toaster />
      <Notify />
    </div>
  )
}

export default page
